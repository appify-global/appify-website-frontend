export const Tags = ({ label }: { label: string }) => {
    return (
        <span className="px-3 py-1.5 sm:px-4 sm:py-2 md:px-5 md:py-3 bg-[#242424] text-white text-[11px] sm:text-xs md:text-md rounded-full">{label}</span>
    )
}
