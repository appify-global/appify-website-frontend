/**
 * Layout Components
 * 
 * Wrapper components that provide consistent structure across pages.
 * Contains shared providers, navigation, and footer.
 */

export { PageLayout, default as PageLayoutDefault } from "./PageLayout";

